#!/bin/bash

if [[ -z $DISPLAY ]] && [[ $(tty) = /dev/tty1 ]]; then
    # Wait for the X server to be ready
    echo "Waiting for X server to start - $(date)"
    while [ ! -x /tmp/.X11-unix/X0 ]; do
        sleep 0.001
    done
    startx
fi
